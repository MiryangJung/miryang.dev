import { ResumeTitleContainer } from './ResumeTitle.style'

const ResumeTitle = ({ title }: { title: string }) => {
  return <ResumeTitleContainer>{title}</ResumeTitleContainer>
}

export default ResumeTitle
